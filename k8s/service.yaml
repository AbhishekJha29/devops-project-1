apiVersion: v1
kind: Service
metadata:
  name: cicd-demo-app-service
  labels:
    app: cicd-demo-app
spec:
  type: NodePort
  selector:
    app: cicd-demo-app
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
